import {describe,expect,it} from "vitest";import {accessRequestSchema,canTransitionLead,formatPlanPrice,publicPlan,whatsappUrl} from "./acquisition";
const valid={academyName:"Amanah Academy",contactName:"Fictional Owner",email:"owner@example.test",whatsapp:"+923001234567",studentCount:25,teacherCount:3,subjects:["Qaida"],acknowledged:"yes",website:""};
describe("academy acquisition",()=>{it("validates a complete access request",()=>expect(accessRequestSchema.safeParse(valid).success).toBe(true));it("rejects invalid international phones and honeypot submissions",()=>{expect(accessRequestSchema.safeParse({...valid,whatsapp:"03001234567"}).success).toBe(false);expect(accessRequestSchema.safeParse({...valid,website:"spam"}).success).toBe(false)});it("normalizes duplicate emails",()=>expect(accessRequestSchema.parse({...valid,email:" OWNER@EXAMPLE.TEST "}).email).toBe("owner@example.test"));it("enforces lead transitions",()=>{expect(canTransitionLead("new","contacted")).toBe(true);expect(canTransitionLead("onboarded","new")).toBe(false)});it("shows only active non-trial plans without inventing prices",()=>{expect(publicPlan({active:true,name:"Starter"})).toBe(true);expect(publicPlan({active:true,name:"Trial"})).toBe(false);expect(publicPlan({active:false,name:"Private"})).toBe(false);expect(formatPlanPrice(null,null)).toBe("Contact us for pricing")});it("builds encoded WhatsApp handoffs",()=>expect(whatsappUrl("+92 300 1234567","Hello academy")).toContain("wa.me/923001234567?text=Hello%20academy"))});
