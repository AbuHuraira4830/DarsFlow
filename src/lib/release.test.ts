import{describe,expect,it}from"vitest";import{academyDate,dedupeNotification,deploymentMode,mergeGroupDraft,missingScheduledReports}from"./release";
describe("pilot release rules",()=>{it("uses academy timezone across UTC day boundaries",()=>{expect(academyDate(new Date("2026-01-01T20:30:00Z"),"Asia/Karachi")).toBe("2026-01-02")});it("finds only scheduled past sessions without reports",()=>{const rows=missingScheduledReports({classes:[{id:"c1",name:"Qaida",meetingDays:["Monday"]},{id:"c2",name:"Archived",meetingDays:["Monday"],archivedAt:"x"}],completed:[],timeZone:"UTC",now:new Date("2026-01-06T12:00:00Z"),lookbackDays:2});expect(rows).toEqual([{classId:"c1",className:"Qaida",date:"2026-01-05"}])});it("resumes group drafts without duplicates",()=>{expect(mergeGroupDraft(["s1","s2"],[{studentId:"s1",value:1}],[{studentId:"s2",value:2}])).toHaveLength(2);expect(()=>mergeGroupDraft(["s1"],[],[{studentId:"s1"},{studentId:"s1"}])).toThrow("Duplicate")});it("deduplicates notification keys",()=>{expect(dedupeNotification(["a"],"a")).toEqual(["a"])});it("selects honest deployment modes",()=>{expect(deploymentMode({databaseUrl:"postgres://db"})).toBe("private_invitation_only");expect(deploymentMode({databaseUrl:"postgres://db",resendKey:"re_x",emailFrom:"pilot@example.com"})).toBe("full_public");expect(deploymentMode({})).toBe("local")})});
