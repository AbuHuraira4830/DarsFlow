"use client";
import {useState} from "react";import {Eye,EyeOff,LockKeyhole} from "lucide-react";
export function PasswordInput({name,label,autoComplete,help}:{name:string;label:string;autoComplete:string;help?:string}){const[shown,setShown]=useState(false);const id=`password-${name}`;return <div><label htmlFor={id} className="block text-sm font-bold text-slate-700">{label}</label><div className="relative mt-2"><LockKeyhole aria-hidden className="pointer-events-none absolute left-3.5 top-1/2 size-4 -translate-y-1/2 text-slate-400"/><input id={id} required minLength={10} name={name} type={shown?"text":"password"} autoComplete={autoComplete} className="min-h-12 w-full rounded-xl border border-slate-300 bg-white pl-10 pr-12 outline-none transition focus:border-teal-600 focus:ring-4 focus:ring-teal-600/10"/><button type="button" onClick={()=>setShown(value=>!value)} aria-label={shown?"Hide password":"Show password"} className="absolute right-1.5 top-1/2 grid size-9 -translate-y-1/2 place-items-center rounded-lg text-slate-500 hover:bg-slate-100 focus-visible:outline-none focus-visible:ring-2 focus-visible:ring-teal-600">{shown?<EyeOff className="size-4"/>:<Eye className="size-4"/>}</button></div>{help&&<p className="mt-1.5 text-xs font-normal leading-5 text-slate-500">{help}</p>}</div>}
