import { createAcademy } from "./actions";
import { requireUser } from "@/server/session";
const tracks = ["Qaida", "Quran Reading", "Hifz", "Tajweed", "Arabic", "Islamic Studies"];
export default async function OnboardingPage({ searchParams }: { searchParams: Promise<{ error?: string }> }) { await requireUser(); const { error } = await searchParams; return <main className="min-h-screen bg-[#f4f8f7] p-5 py-10"><form action={createAcademy} className="mx-auto max-w-2xl rounded-3xl border border-slate-200 bg-white p-6 shadow-xl sm:p-10"><p className="text-sm font-bold text-teal-700">Academy setup</p><h1 className="mt-2 text-3xl font-semibold">Create your workspace</h1><p className="mt-2 text-slate-600">These details control schedules, teaching options and academy-specific records.</p>{error && <p role="alert" className="mt-5 rounded-xl bg-rose-50 p-3 text-sm font-bold text-rose-800">{error}</p>}<div className="mt-7 grid gap-5 sm:grid-cols-2"><Input name="name" label="Academy name" /><label className="text-sm font-bold text-slate-700">Timezone<select name="timezone" defaultValue="Asia/Karachi" className="mt-2 min-h-12 w-full rounded-xl border border-slate-300 bg-white px-3"><option>Asia/Karachi</option><option>Europe/London</option><option>America/New_York</option><option>Asia/Dubai</option></select></label><Input name="supportPhone" label="Support number (optional)" required={false} /></div><fieldset className="mt-6"><legend className="text-sm font-bold text-slate-700">Teaching tracks</legend><div className="mt-3 grid gap-2 sm:grid-cols-2">{tracks.map((track) => <label key={track} className="flex min-h-11 items-center gap-3 rounded-xl border border-slate-200 px-3"><input type="checkbox" name="tracks" value={track} defaultChecked={track === "Qaida" || track === "Quran Reading"} />{track}</label>)}</div></fieldset><button className="mt-8 min-h-12 w-full rounded-xl bg-teal-700 font-bold text-white">Finish setup</button></form></main>; }
function Input({ name, label, required = true }: { name: string; label: string; required?: boolean }) { return <label className="text-sm font-bold text-slate-700">{label}<input name={name} required={required} className="mt-2 min-h-12 w-full rounded-xl border border-slate-300 px-4" /></label>; }
