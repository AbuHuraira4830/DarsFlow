import { ResetPassword } from "@/components/password-recovery";export default async function ResetPage({searchParams}:{searchParams:Promise<{token?:string}>}){const{token=''}=await searchParams;return <main className="grid min-h-screen place-items-center bg-[#f5f8f7] p-5"><section className="w-full max-w-md rounded-2xl border bg-white p-7"><h1 className="text-3xl font-semibold">Choose a new password</h1>{token?<ResetPassword token={token}/>:<p className="mt-4 text-rose-700">This reset link is incomplete.</p>}</section></main>}
