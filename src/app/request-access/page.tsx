import {AccessRequestForm} from "@/components/access-request-form";import {PublicPage} from "@/components/public-shell";import {db} from "@/server/db";import {platformSettings} from "@/server/schema";
export const dynamic="force-dynamic";
export default async function RequestAccess(){const settings=await db.select().from(platformSettings).then(r=>r[0]);return <PublicPage settings={settings}><div className="mx-auto grid max-w-6xl gap-8 px-5 py-10 lg:grid-cols-[.75fr_1.25fr] lg:py-16"><div><p className="text-sm font-bold text-teal-700">Invitation-only access</p><h1 className="mt-3 text-4xl font-semibold tracking-tight">Tell us about your academy.</h1><p className="mt-4 leading-7 text-slate-600">We manually review each request so the pilot remains useful and well supported. No payment or automated email is involved.</p><ul className="mt-7 space-y-3 text-sm text-slate-700"><li>• Explore your teaching workflow with the public fictional demo.</li><li>• Receive a secure, expiring invitation if approved.</li><li>• Set up teachers, students, classes and your first lesson update.</li></ul></div><AccessRequestForm/></div></PublicPage>}
