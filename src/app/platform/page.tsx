import { count,eq } from "drizzle-orm";import { redirect } from "next/navigation";import { DarsFlowLogo } from "@/components/darsflow-logo";import { Card,Empty,PageHeader,button,input } from "@/components/workspace-ui";import { db } from "@/server/db";import { academies,memberships,paymentRequests,platformSettings,students } from "@/server/schema";import { isPlatformAdmin,requireUser } from "@/server/session";import { reviewPayment,savePaymentInstructions } from "./actions";
export default async function Platform(){const session=await requireUser();if(!isPlatformAdmin(session.user.email))redirect('/app');const academyList=db.select().from(academies).all();const payments=db.select().from(paymentRequests).where(eq(paymentRequests.status,'pending')).all();const settings=db.select().from(platformSettings).where(eq(platformSettings.id,'default')).get();return <main className="min-h-screen bg-[#f5f8f7] p-5 sm:p-8"><div className="mx-auto max-w-6xl"><DarsFlowLogo/><div className="mt-8"><PageHeader eyebrow="Restricted platform administration" title="DarsFlow operations" description="Access is controlled only through PLATFORM_ADMIN_EMAILS; no default administrator password exists."/></div><div className="grid gap-5 lg:grid-cols-2"><Card title="Academies">{academyList.map(a=><div key={a.id} className="border-b py-3"><strong>{a.name}</strong><p className="text-xs text-slate-500">{a.status} · {db.select({n:count()}).from(students).where(eq(students.academyId,a.id)).get()?.n??0} students · {db.select({n:count()}).from(memberships).where(eq(memberships.academyId,a.id)).get()?.n??0} members</p></div>)}</Card><Card title="Payment instructions" tone="sand"><form action={savePaymentInstructions}><textarea name="instructions" defaultValue={settings?.paymentInstructions??''} placeholder="Configure real bank, Easypaisa or JazzCash instructions only when available." className={`${input} min-h-28 py-3`}/><button className={`${button} mt-3`}>Save instructions</button></form></Card></div><Card title="Pending payment verification">{payments.length?payments.map(p=><form action={reviewPayment} key={p.id} className="mt-3 rounded-xl border p-4"><input type="hidden" name="id" value={p.id}/><p><strong>{p.reference}</strong> · academy {p.academyId}</p><input name="reason" placeholder="Reason if rejected" className={input}/><div className="mt-3 flex gap-2"><button name="decision" value="approve" className={button}>Approve</button><button name="decision" value="reject" className="rounded-xl border px-4 font-bold">Reject</button></div></form>):<Empty>No payment requests awaiting review.</Empty>}</Card></div></main>}
